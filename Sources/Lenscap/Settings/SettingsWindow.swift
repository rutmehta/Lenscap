import AppKit

/// Settings window. Skeleton — the full SwiftUI tabbed settings UI
/// (general, shortcuts, recording) lives in a follow-up pass.
@MainActor
final class SettingsWindowController {
    static func open() {
        HUD.show("Settings UI is not implemented yet", symbol: "gearshape")
    }
}
